using System;
using System.Collections.Generic;
using System.Text;

namespace DAL
{
    public enum SQLType
    {
        SQLServer,
        MySql
    };

    public enum NoSQLType
    {
        MongoDB
    };
}
